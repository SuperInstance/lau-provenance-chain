[package]
name = "lau-provenance-chain"
version = "0.1.0"
edition = "2021"
description = "Full provenance chains — every decision, every alternative considered, every reason why"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
